
new trace, ret, ch, lookup(`rho:registry:lookup`) in {
   for (@{"read": *MCA, ..._} <<- @[*deployerId, "MasterContractAdmin"]) {
      trace!({"MCA": MCA}) |
      MCA("Roll", ch)
   } |
   for (@set <- ch) {
      ret!(["#define", "$roll", set.toList()])
   }
}
